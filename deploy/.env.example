# Copy to deploy/.env and fill in. DOMAIN is required.

# A public domain whose DNS A-record points at this VM. Caddy gets a TLS cert for it.
DOMAIN=api.example.com

# Optional — Stripe billing webhook (set once you create the webhook in Stripe).
DPROV_STRIPE_WEBHOOK_SECRET=
DPROV_STRIPE_PRICE_PLANS=
